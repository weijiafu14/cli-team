复核结果：SQLite 数据与 coord 文件现在一致，team 7e0a4e74 的 extra.members 已包含 Gemini（memberId=94ae93e5, conversationId=0c012bc0），并且已存在新的 gemini 子会话记录 extra.teamId=7e0a4e74。数据层校验通过。剩余风险：当前进程中的 CoordDispatcher 在 startDispatcher() 时一次性读取 teamConversation.extra.members，并且 dispatchers.has(teamId) 时不会重建；也就是说，如果该 team 的 dispatcher 早已启动，手改数据库后它未必自动吸收新成员。要让 Gemini 立即参与当前运行中的团队，通常还需要重启应用，或显式 stop/start 该 team 的 dispatcher。